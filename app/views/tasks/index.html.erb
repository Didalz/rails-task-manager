<h1>ÉSTAS SON TUS TAREAS</h1>
<% @tasks.each do |task| %>
    <li>
    <%= link_to task.title, task_path(task) %>
    <%= link_to "esfumate", task_path(task), data: {turbo_method: :delete, turbo_confirm: "are you sure?"}%>
    </li>
<% end %>

<h3><%= link_to "Añadir nueva tarea", new_path %></h3>
